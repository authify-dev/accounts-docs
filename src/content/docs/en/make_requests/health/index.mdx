---
title: Health
description: Ejecucion en local de la API
arguments:
  required:
    - name: "token"
      type: "token"
      isRequired: true
      description: "Authentication token bearing required scopes. Tokens should be passed as an HTTP Authorization header or alternatively, as a POST parameter."
      example: "xxxx-xxxxxxxxx-xxxx"
    - name: "app_ids"
      type: "array"
      isRequired: true
      description: "An array of app IDs to get app configs for"
      example: "A0A2R51A5,A0A2R51A6"
  optional:
    - name: "app_ids"
      type: "array"
      isRequired: true
      description: "An array of app IDs to get app configs for"
      example: "A0A2R51A5,A0A2R51A6"
---

import EndpointLayout from "@layouts/EndpointLayout.astro";
import { ArgumentEntity } from "src/domain/entities/ArgumentEntity";
import { Tabs, TabItem } from "@astrojs/starlight/components";

import RequestSection from "./sections/request.mdx";
import HeadersSection from "./sections/headers.mdx";
import ResponsesSection from "./sections/responses.mdx";
import ErrorsSection from "./sections/errors.mdx";
import WarningsSection from "./sections/warnings.mdx";


<EndpointLayout
  method="GET"
  path="/health"
  description="This endpoint provides a service health check, allowing you to verify its availability and correct operation in real time. It is useful for monitoring the operational status of the system and ensuring service continuity for dependent users and applications."
>
  <div slot="request-section">
    <RequestSection />
  </div>
  <div slot="headers-section">
    <HeadersSection />
  </div>
  <div slot="responses-section">
    <ResponsesSection />
  </div>

</EndpointLayout>

